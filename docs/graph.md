<!-- Generated by tools/build.py. Edit catalog/*.toml, not this file. -->

# Agent graph

## The loop every agent runs

```mermaid
flowchart LR
  P[Plan] --> A[Act] --> V[Verify] --> R[Reflect]
  R -->|progress, budget left| P
  R -->|criteria met with evidence| D([Report])
  V -->|same failure twice| S([Stop: report])
  A -->|irreversible action| G([Stop: ask user])
  D -->|handoff| H{{Main session routes to next agent}}
```

## Departments and cross-department handoffs

Edge labels count handoffs from agents in one department to agents in another.

```mermaid
flowchart LR
  agent_ops[Agent Ops · 11]
  ai_engineering[AI Engineering · 10]
  architecture[Architecture · 10]
  code_review[Code Review · 10]
  devops[DevOps & Release · 10]
  docs[Docs & Knowledge · 10]
  explainable_ai[Explainable AI · 10]
  quality[Quality · 10]
  risk_reward[Risk & Reward · 10]
  safety[Safety · 10]
  token_economy[Token Economy · 10]
  agent_ops -->|1| ai_engineering
  agent_ops -->|1| devops
  agent_ops -->|1| quality
  agent_ops -->|1| risk_reward
  agent_ops -->|1| safety
  agent_ops -->|2| token_economy
  ai_engineering -->|1| agent_ops
  ai_engineering -->|1| explainable_ai
  ai_engineering -->|2| risk_reward
  ai_engineering -->|5| safety
  ai_engineering -->|6| token_economy
  architecture -->|1| agent_ops
  architecture -->|2| code_review
  architecture -->|4| devops
  architecture -->|1| docs
  architecture -->|2| explainable_ai
  architecture -->|2| quality
  architecture -->|5| risk_reward
  architecture -->|3| safety
  code_review -->|1| agent_ops
  code_review -->|4| architecture
  code_review -->|2| devops
  code_review -->|1| docs
  code_review -->|3| explainable_ai
  code_review -->|3| quality
  code_review -->|4| risk_reward
  code_review -->|1| safety
  devops -->|1| agent_ops
  devops -->|1| architecture
  devops -->|1| code_review
  devops -->|1| docs
  devops -->|1| quality
  devops -->|8| risk_reward
  devops -->|4| safety
  devops -->|2| token_economy
  docs -->|1| agent_ops
  docs -->|2| architecture
  docs -->|3| code_review
  docs -->|2| devops
  docs -->|1| explainable_ai
  docs -->|1| quality
  explainable_ai -->|3| agent_ops
  explainable_ai -->|2| ai_engineering
  explainable_ai -->|2| code_review
  explainable_ai -->|1| devops
  explainable_ai -->|2| docs
  explainable_ai -->|1| quality
  explainable_ai -->|2| risk_reward
  explainable_ai -->|2| token_economy
  quality -->|1| agent_ops
  quality -->|3| code_review
  quality -->|2| devops
  quality -->|1| risk_reward
  quality -->|1| safety
  quality -->|2| token_economy
  risk_reward -->|1| agent_ops
  risk_reward -->|3| architecture
  risk_reward -->|2| code_review
  risk_reward -->|4| devops
  risk_reward -->|4| explainable_ai
  risk_reward -->|1| quality
  risk_reward -->|3| safety
  safety -->|1| agent_ops
  safety -->|4| ai_engineering
  safety -->|5| devops
  safety -->|1| quality
  safety -->|4| risk_reward
  token_economy -->|8| agent_ops
  token_economy -->|5| ai_engineering
  token_economy -->|1| architecture
  token_economy -->|1| code_review
  token_economy -->|1| devops
  token_economy -->|1| explainable_ai
  token_economy -->|1| risk_reward
  token_economy -->|1| safety
```

## Per-department handoff graphs

### Agent Ops

```mermaid
flowchart LR
  subgraph PLAN[plan]
    loop_planner[loop-planner]
    budget_controller[budget-controller]
    handoff_router[handoff-router]
  end
  subgraph VERIFY[verify]
    loop_verifier[loop-verifier]
    agent_auditor[agent-auditor]
    eval_designer[eval-designer]
  end
  subgraph REFLECT[reflect]
    loop_reflector[loop-reflector]
    stop_condition_judge[stop-condition-judge]
    failure_analyst[failure-analyst]
    memory_curator[memory-curator]
    skill_extractor[skill-extractor]
  end
  loop_planner --> budget_controller
  loop_planner --> handoff_router
  loop_planner --> change_risk_scorer
  loop_verifier --> failure_analyst
  loop_verifier --> loop_reflector
  loop_reflector --> memory_curator
  loop_reflector --> loop_planner
  loop_reflector --> stop_condition_judge
  budget_controller --> context_budgeter
  budget_controller --> model_router
  handoff_router --> loop_planner
  stop_condition_judge --> loop_reflector
  failure_analyst --> flaky_test_hunter
  failure_analyst --> ci_doctor
  failure_analyst --> loop_planner
  memory_curator --> skill_extractor
  skill_extractor --> agent_auditor
  agent_auditor --> permission_minimizer
  agent_auditor --> eval_designer
  eval_designer --> llm_eval_builder
  change_risk_scorer([change-risk-scorer · risk-reward])
  ci_doctor([ci-doctor · devops])
  context_budgeter([context-budgeter · token-economy])
  flaky_test_hunter([flaky-test-hunter · quality])
  llm_eval_builder([llm-eval-builder · ai-engineering])
  model_router([model-router · token-economy])
  permission_minimizer([permission-minimizer · safety])
```

### AI Engineering

```mermaid
flowchart LR
  subgraph PLAN[plan]
    llm_cost_estimator[llm-cost-estimator]
    guardrail_designer[guardrail-designer]
    agent_architect[agent-architect]
    context_engineer[context-engineer]
  end
  subgraph ACT[act]
    prompt_engineer[prompt-engineer]
    tool_schema_designer[tool-schema-designer]
  end
  subgraph VERIFY[verify]
    llm_eval_builder[llm-eval-builder]
    rag_reviewer[rag-reviewer]
    hallucination_checker[hallucination-checker]
    prompt_regression_tester[prompt-regression-tester]
  end
  llm_eval_builder --> prompt_regression_tester
  llm_eval_builder --> llm_cost_estimator
  llm_eval_builder --> hallucination_checker
  prompt_engineer --> llm_eval_builder
  prompt_engineer --> prompt_compressor
  rag_reviewer --> retrieval_scoper
  rag_reviewer --> hallucination_checker
  rag_reviewer --> prompt_injection_reviewer
  hallucination_checker --> rag_reviewer
  hallucination_checker --> guardrail_designer
  hallucination_checker --> confidence_calibrator
  llm_cost_estimator --> cache_planner
  llm_cost_estimator --> model_router
  llm_cost_estimator --> cost_benefit_analyst
  tool_schema_designer --> agent_architect
  tool_schema_designer --> permission_minimizer
  guardrail_designer --> prompt_injection_reviewer
  guardrail_designer --> llm_output_safety_reviewer
  guardrail_designer --> pii_detector
  agent_architect --> tool_schema_designer
  agent_architect --> context_engineer
  agent_architect --> loop_planner
  prompt_regression_tester --> prompt_engineer
  prompt_regression_tester --> release_gatekeeper
  context_engineer --> context_budgeter
  context_engineer --> cache_planner
  context_engineer --> llm_eval_builder
  cache_planner([cache-planner · token-economy])
  confidence_calibrator([confidence-calibrator · explainable-ai])
  context_budgeter([context-budgeter · token-economy])
  cost_benefit_analyst([cost-benefit-analyst · risk-reward])
  llm_output_safety_reviewer([llm-output-safety-reviewer · safety])
  loop_planner([loop-planner · agent-ops])
  model_router([model-router · token-economy])
  permission_minimizer([permission-minimizer · safety])
  pii_detector([pii-detector · safety])
  prompt_compressor([prompt-compressor · token-economy])
  prompt_injection_reviewer([prompt-injection-reviewer · safety])
  release_gatekeeper([release-gatekeeper · risk-reward])
  retrieval_scoper([retrieval-scoper · token-economy])
```

### Architecture

```mermaid
flowchart LR
  subgraph PLAN[plan]
    module_mapper[module-mapper]
    dependency_grapher[dependency-grapher]
    refactor_planner[refactor-planner]
    interface_designer[interface-designer]
    migration_planner[migration-planner]
    monorepo_organizer[monorepo-organizer]
    tech_stack_advisor[tech-stack-advisor]
  end
  subgraph VERIFY[verify]
    boundary_enforcer[boundary-enforcer]
    data_model_reviewer[data-model-reviewer]
    scalability_reviewer[scalability-reviewer]
  end
  module_mapper --> dependency_grapher
  module_mapper --> diagram_maker
  module_mapper --> memory_curator
  dependency_grapher --> boundary_enforcer
  dependency_grapher --> refactor_planner
  boundary_enforcer --> ci_doctor
  boundary_enforcer --> adr_writer
  refactor_planner --> test_gap_finder
  refactor_planner --> blast_radius_mapper
  refactor_planner --> feature_flag_planner
  interface_designer --> api_contract_reviewer
  interface_designer --> contract_test_writer
  data_model_reviewer --> migration_planner
  data_model_reviewer --> pii_detector
  scalability_reviewer --> performance_reviewer
  scalability_reviewer --> observability_planner
  scalability_reviewer --> infra_cost_reviewer
  migration_planner --> migration_risk_reviewer
  migration_planner --> rollback_planner
  migration_planner --> data_deletion_guard
  monorepo_organizer --> build_accelerator
  monorepo_organizer --> dependency_grapher
  tech_stack_advisor --> license_checker
  tech_stack_advisor --> dependency_risk_auditor
  tech_stack_advisor --> adr_writer
  adr_writer([adr-writer · explainable-ai])
  api_contract_reviewer([api-contract-reviewer · code-review])
  blast_radius_mapper([blast-radius-mapper · risk-reward])
  build_accelerator([build-accelerator · devops])
  ci_doctor([ci-doctor · devops])
  contract_test_writer([contract-test-writer · quality])
  data_deletion_guard([data-deletion-guard · safety])
  dependency_risk_auditor([dependency-risk-auditor · risk-reward])
  diagram_maker([diagram-maker · docs])
  feature_flag_planner([feature-flag-planner · risk-reward])
  infra_cost_reviewer([infra-cost-reviewer · devops])
  license_checker([license-checker · safety])
  memory_curator([memory-curator · agent-ops])
  migration_risk_reviewer([migration-risk-reviewer · risk-reward])
  observability_planner([observability-planner · devops])
  performance_reviewer([performance-reviewer · code-review])
  pii_detector([pii-detector · safety])
  rollback_planner([rollback-planner · risk-reward])
  test_gap_finder([test-gap-finder · quality])
```

### Code Review

```mermaid
flowchart LR
  subgraph PLAN[plan]
    dead_code_finder[dead-code-finder]
  end
  subgraph ACT[act]
    simplifier[simplifier]
  end
  subgraph VERIFY[verify]
    bug_hunter[bug-hunter]
    api_contract_reviewer[api-contract-reviewer]
    concurrency_reviewer[concurrency-reviewer]
    error_handling_reviewer[error-handling-reviewer]
    performance_reviewer[performance-reviewer]
    readability_reviewer[readability-reviewer]
  end
  subgraph REFLECT[reflect]
    pr_describer[pr-describer]
    review_triager[review-triager]
  end
  bug_hunter --> regression_test_writer
  bug_hunter --> concurrency_reviewer
  bug_hunter --> confidence_calibrator
  api_contract_reviewer --> versioning_advisor
  api_contract_reviewer --> contract_test_writer
  api_contract_reviewer --> blast_radius_mapper
  concurrency_reviewer --> regression_test_writer
  concurrency_reviewer --> data_model_reviewer
  error_handling_reviewer --> observability_planner
  error_handling_reviewer --> secrets_scanner
  performance_reviewer --> scalability_reviewer
  performance_reviewer --> data_model_reviewer
  readability_reviewer --> simplifier
  readability_reviewer --> docstring_writer
  simplifier --> loop_verifier
  simplifier --> refactor_planner
  dead_code_finder --> api_contract_reviewer
  dead_code_finder --> dependency_risk_auditor
  pr_describer --> change_narrator
  pr_describer --> change_risk_scorer
  review_triager --> reviewer_briefer
  review_triager --> release_gatekeeper
  blast_radius_mapper([blast-radius-mapper · risk-reward])
  change_narrator([change-narrator · explainable-ai])
  change_risk_scorer([change-risk-scorer · risk-reward])
  confidence_calibrator([confidence-calibrator · explainable-ai])
  contract_test_writer([contract-test-writer · quality])
  data_model_reviewer([data-model-reviewer · architecture])
  dependency_risk_auditor([dependency-risk-auditor · risk-reward])
  docstring_writer([docstring-writer · docs])
  loop_verifier([loop-verifier · agent-ops])
  observability_planner([observability-planner · devops])
  refactor_planner([refactor-planner · architecture])
  regression_test_writer([regression-test-writer · quality])
  release_gatekeeper([release-gatekeeper · risk-reward])
  reviewer_briefer([reviewer-briefer · explainable-ai])
  scalability_reviewer([scalability-reviewer · architecture])
  secrets_scanner([secrets-scanner · safety])
  versioning_advisor([versioning-advisor · devops])
```

### DevOps & Release

```mermaid
flowchart LR
  subgraph PLAN[plan]
    build_accelerator[build-accelerator]
    versioning_advisor[versioning-advisor]
    observability_planner[observability-planner]
  end
  subgraph ACT[act]
    release_notes_writer[release-notes-writer]
  end
  subgraph VERIFY[verify]
    dockerfile_reviewer[dockerfile-reviewer]
    env_config_auditor[env-config-auditor]
    deploy_checklist_runner[deploy-checklist-runner]
  end
  subgraph REFLECT[reflect]
    ci_doctor[ci-doctor]
    incident_responder[incident-responder]
    infra_cost_reviewer[infra-cost-reviewer]
  end
  ci_doctor --> flaky_test_hunter
  ci_doctor --> log_trimmer
  ci_doctor --> build_accelerator
  build_accelerator --> dockerfile_reviewer
  build_accelerator --> monorepo_organizer
  build_accelerator --> cache_planner
  release_notes_writer --> versioning_advisor
  release_notes_writer --> changelog_keeper
  versioning_advisor --> api_contract_reviewer
  versioning_advisor --> release_gatekeeper
  dockerfile_reviewer --> secrets_scanner
  dockerfile_reviewer --> supply_chain_auditor
  env_config_auditor --> secrets_scanner
  env_config_auditor --> deploy_checklist_runner
  deploy_checklist_runner --> release_gatekeeper
  deploy_checklist_runner --> rollback_planner
  deploy_checklist_runner --> migration_risk_reviewer
  observability_planner --> pii_detector
  observability_planner --> infra_cost_reviewer
  incident_responder --> rollback_planner
  incident_responder --> failure_analyst
  incident_responder --> premortem_facilitator
  infra_cost_reviewer --> cost_benefit_analyst
  infra_cost_reviewer --> blast_radius_mapper
  api_contract_reviewer([api-contract-reviewer · code-review])
  blast_radius_mapper([blast-radius-mapper · risk-reward])
  cache_planner([cache-planner · token-economy])
  changelog_keeper([changelog-keeper · docs])
  cost_benefit_analyst([cost-benefit-analyst · risk-reward])
  failure_analyst([failure-analyst · agent-ops])
  flaky_test_hunter([flaky-test-hunter · quality])
  log_trimmer([log-trimmer · token-economy])
  migration_risk_reviewer([migration-risk-reviewer · risk-reward])
  monorepo_organizer([monorepo-organizer · architecture])
  pii_detector([pii-detector · safety])
  premortem_facilitator([premortem-facilitator · risk-reward])
  release_gatekeeper([release-gatekeeper · risk-reward])
  rollback_planner([rollback-planner · risk-reward])
  secrets_scanner([secrets-scanner · safety])
  supply_chain_auditor([supply-chain-auditor · safety])
```

### Docs & Knowledge

```mermaid
flowchart LR
  subgraph ACT[act]
    readme_writer[readme-writer]
    api_doc_writer[api-doc-writer]
    onboarding_guide_writer[onboarding-guide-writer]
    changelog_keeper[changelog-keeper]
    docstring_writer[docstring-writer]
    glossary_builder[glossary-builder]
    tutorial_writer[tutorial-writer]
    diagram_maker[diagram-maker]
    faq_builder[faq-builder]
  end
  subgraph VERIFY[verify]
    doc_drift_detector[doc-drift-detector]
  end
  readme_writer --> onboarding_guide_writer
  readme_writer --> diagram_maker
  api_doc_writer --> api_contract_reviewer
  api_doc_writer --> contract_test_writer
  onboarding_guide_writer --> glossary_builder
  onboarding_guide_writer --> module_mapper
  changelog_keeper --> release_notes_writer
  changelog_keeper --> versioning_advisor
  docstring_writer --> api_doc_writer
  docstring_writer --> readability_reviewer
  glossary_builder --> memory_curator
  tutorial_writer --> doc_drift_detector
  tutorial_writer --> faq_builder
  doc_drift_detector --> readme_writer
  doc_drift_detector --> api_doc_writer
  diagram_maker --> dependency_grapher
  diagram_maker --> adr_writer
  faq_builder --> readme_writer
  faq_builder --> bug_hunter
  adr_writer([adr-writer · explainable-ai])
  api_contract_reviewer([api-contract-reviewer · code-review])
  bug_hunter([bug-hunter · code-review])
  contract_test_writer([contract-test-writer · quality])
  dependency_grapher([dependency-grapher · architecture])
  memory_curator([memory-curator · agent-ops])
  module_mapper([module-mapper · architecture])
  readability_reviewer([readability-reviewer · code-review])
  release_notes_writer([release-notes-writer · devops])
  versioning_advisor([versioning-advisor · devops])
```

### Explainable AI

```mermaid
flowchart LR
  subgraph PLAN[plan]
    assumption_surfacer[assumption-surfacer]
  end
  subgraph ACT[act]
    adr_writer[adr-writer]
    model_card_writer[model-card-writer]
  end
  subgraph VERIFY[verify]
    evidence_linker[evidence-linker]
    confidence_calibrator[confidence-calibrator]
    reviewer_briefer[reviewer-briefer]
  end
  subgraph REFLECT[reflect]
    decision_explainer[decision-explainer]
    change_narrator[change-narrator]
    trace_reporter[trace-reporter]
    alternatives_recorder[alternatives-recorder]
  end
  decision_explainer --> adr_writer
  decision_explainer --> alternatives_recorder
  change_narrator --> pr_describer
  change_narrator --> reviewer_briefer
  change_narrator --> diff_summarizer
  evidence_linker --> loop_verifier
  evidence_linker --> confidence_calibrator
  assumption_surfacer --> premortem_facilitator
  assumption_surfacer --> property_test_designer
  confidence_calibrator --> evidence_linker
  confidence_calibrator --> loop_verifier
  trace_reporter --> log_trimmer
  trace_reporter --> incident_responder
  alternatives_recorder --> adr_writer
  alternatives_recorder --> memory_curator
  adr_writer --> diagram_maker
  adr_writer --> doc_drift_detector
  model_card_writer --> llm_eval_builder
  model_card_writer --> guardrail_designer
  reviewer_briefer --> change_risk_scorer
  reviewer_briefer --> review_triager
  change_risk_scorer([change-risk-scorer · risk-reward])
  diagram_maker([diagram-maker · docs])
  diff_summarizer([diff-summarizer · token-economy])
  doc_drift_detector([doc-drift-detector · docs])
  guardrail_designer([guardrail-designer · ai-engineering])
  incident_responder([incident-responder · devops])
  llm_eval_builder([llm-eval-builder · ai-engineering])
  log_trimmer([log-trimmer · token-economy])
  loop_verifier([loop-verifier · agent-ops])
  memory_curator([memory-curator · agent-ops])
  pr_describer([pr-describer · code-review])
  premortem_facilitator([premortem-facilitator · risk-reward])
  property_test_designer([property-test-designer · quality])
  review_triager([review-triager · code-review])
```

### Quality

```mermaid
flowchart LR
  subgraph PLAN[plan]
    test_gap_finder[test-gap-finder]
    e2e_scenario_planner[e2e-scenario-planner]
  end
  subgraph ACT[act]
    regression_test_writer[regression-test-writer]
    property_test_designer[property-test-designer]
    fixture_builder[fixture-builder]
    contract_test_writer[contract-test-writer]
  end
  subgraph VERIFY[verify]
    flaky_test_hunter[flaky-test-hunter]
    mutation_test_advisor[mutation-test-advisor]
    coverage_analyst[coverage-analyst]
    snapshot_reviewer[snapshot-reviewer]
  end
  test_gap_finder --> regression_test_writer
  test_gap_finder --> property_test_designer
  test_gap_finder --> change_risk_scorer
  flaky_test_hunter --> fixture_builder
  flaky_test_hunter --> ci_doctor
  flaky_test_hunter --> concurrency_reviewer
  regression_test_writer --> loop_verifier
  regression_test_writer --> mutation_test_advisor
  property_test_designer --> regression_test_writer
  property_test_designer --> bug_hunter
  mutation_test_advisor --> regression_test_writer
  mutation_test_advisor --> coverage_analyst
  coverage_analyst --> test_gap_finder
  coverage_analyst --> log_trimmer
  e2e_scenario_planner --> fixture_builder
  e2e_scenario_planner --> contract_test_writer
  fixture_builder --> pii_detector
  fixture_builder --> flaky_test_hunter
  snapshot_reviewer --> regression_test_writer
  snapshot_reviewer --> diff_summarizer
  contract_test_writer --> api_contract_reviewer
  contract_test_writer --> versioning_advisor
  api_contract_reviewer([api-contract-reviewer · code-review])
  bug_hunter([bug-hunter · code-review])
  change_risk_scorer([change-risk-scorer · risk-reward])
  ci_doctor([ci-doctor · devops])
  concurrency_reviewer([concurrency-reviewer · code-review])
  diff_summarizer([diff-summarizer · token-economy])
  log_trimmer([log-trimmer · token-economy])
  loop_verifier([loop-verifier · agent-ops])
  pii_detector([pii-detector · safety])
  versioning_advisor([versioning-advisor · devops])
```

### Risk & Reward

```mermaid
flowchart LR
  subgraph PLAN[plan]
    blast_radius_mapper[blast-radius-mapper]
    rollback_planner[rollback-planner]
    feature_flag_planner[feature-flag-planner]
    cost_benefit_analyst[cost-benefit-analyst]
    premortem_facilitator[premortem-facilitator]
  end
  subgraph VERIFY[verify]
    change_risk_scorer[change-risk-scorer]
    dependency_risk_auditor[dependency-risk-auditor]
    migration_risk_reviewer[migration-risk-reviewer]
    release_gatekeeper[release-gatekeeper]
  end
  subgraph REFLECT[reflect]
    tech_debt_ledger[tech-debt-ledger]
  end
  change_risk_scorer --> blast_radius_mapper
  change_risk_scorer --> rollback_planner
  change_risk_scorer --> decision_explainer
  blast_radius_mapper --> dependency_grapher
  blast_radius_mapper --> test_gap_finder
  blast_radius_mapper --> api_contract_reviewer
  rollback_planner --> migration_risk_reviewer
  rollback_planner --> feature_flag_planner
  rollback_planner --> deploy_checklist_runner
  dependency_risk_auditor --> supply_chain_auditor
  dependency_risk_auditor --> license_checker
  migration_risk_reviewer --> data_model_reviewer
  migration_risk_reviewer --> data_deletion_guard
  migration_risk_reviewer --> rollback_planner
  feature_flag_planner --> observability_planner
  feature_flag_planner --> tech_debt_ledger
  cost_benefit_analyst --> alternatives_recorder
  cost_benefit_analyst --> adr_writer
  premortem_facilitator --> assumption_surfacer
  premortem_facilitator --> loop_planner
  release_gatekeeper --> release_notes_writer
  release_gatekeeper --> versioning_advisor
  release_gatekeeper --> change_risk_scorer
  tech_debt_ledger --> refactor_planner
  tech_debt_ledger --> dead_code_finder
  adr_writer([adr-writer · explainable-ai])
  alternatives_recorder([alternatives-recorder · explainable-ai])
  api_contract_reviewer([api-contract-reviewer · code-review])
  assumption_surfacer([assumption-surfacer · explainable-ai])
  data_deletion_guard([data-deletion-guard · safety])
  data_model_reviewer([data-model-reviewer · architecture])
  dead_code_finder([dead-code-finder · code-review])
  decision_explainer([decision-explainer · explainable-ai])
  dependency_grapher([dependency-grapher · architecture])
  deploy_checklist_runner([deploy-checklist-runner · devops])
  license_checker([license-checker · safety])
  loop_planner([loop-planner · agent-ops])
  observability_planner([observability-planner · devops])
  refactor_planner([refactor-planner · architecture])
  release_notes_writer([release-notes-writer · devops])
  supply_chain_auditor([supply-chain-auditor · safety])
  test_gap_finder([test-gap-finder · quality])
  versioning_advisor([versioning-advisor · devops])
```

### Safety

```mermaid
flowchart LR
  subgraph PLAN[plan]
    sandbox_advisor[sandbox-advisor]
  end
  subgraph ACT[act]
    permission_minimizer[permission-minimizer]
  end
  subgraph VERIFY[verify]
    secrets_scanner[secrets-scanner]
    destructive_command_guard[destructive-command-guard]
    prompt_injection_reviewer[prompt-injection-reviewer]
    pii_detector[pii-detector]
    license_checker[license-checker]
    supply_chain_auditor[supply-chain-auditor]
    data_deletion_guard[data-deletion-guard]
    llm_output_safety_reviewer[llm-output-safety-reviewer]
  end
  secrets_scanner --> env_config_auditor
  secrets_scanner --> incident_responder
  destructive_command_guard --> data_deletion_guard
  destructive_command_guard --> permission_minimizer
  permission_minimizer --> sandbox_advisor
  permission_minimizer --> agent_auditor
  prompt_injection_reviewer --> guardrail_designer
  prompt_injection_reviewer --> tool_schema_designer
  pii_detector --> fixture_builder
  pii_detector --> observability_planner
  license_checker --> supply_chain_auditor
  license_checker --> dependency_risk_auditor
  supply_chain_auditor --> dependency_risk_auditor
  supply_chain_auditor --> ci_doctor
  sandbox_advisor --> permission_minimizer
  sandbox_advisor --> dockerfile_reviewer
  data_deletion_guard --> migration_risk_reviewer
  data_deletion_guard --> rollback_planner
  llm_output_safety_reviewer --> guardrail_designer
  llm_output_safety_reviewer --> hallucination_checker
  agent_auditor([agent-auditor · agent-ops])
  ci_doctor([ci-doctor · devops])
  dependency_risk_auditor([dependency-risk-auditor · risk-reward])
  dockerfile_reviewer([dockerfile-reviewer · devops])
  env_config_auditor([env-config-auditor · devops])
  fixture_builder([fixture-builder · quality])
  guardrail_designer([guardrail-designer · ai-engineering])
  hallucination_checker([hallucination-checker · ai-engineering])
  incident_responder([incident-responder · devops])
  migration_risk_reviewer([migration-risk-reviewer · risk-reward])
  observability_planner([observability-planner · devops])
  rollback_planner([rollback-planner · risk-reward])
  tool_schema_designer([tool-schema-designer · ai-engineering])
```

### Token Economy

```mermaid
flowchart LR
  subgraph PLAN[plan]
    context_budgeter[context-budgeter]
    cache_planner[cache-planner]
    model_router[model-router]
    output_shaper[output-shaper]
  end
  subgraph ACT[act]
    prompt_compressor[prompt-compressor]
    diff_summarizer[diff-summarizer]
    log_trimmer[log-trimmer]
    retrieval_scoper[retrieval-scoper]
  end
  subgraph REFLECT[reflect]
    session_summarizer[session-summarizer]
    tool_call_auditor[tool-call-auditor]
  end
  context_budgeter --> retrieval_scoper
  context_budgeter --> budget_controller
  prompt_compressor --> cache_planner
  prompt_compressor --> memory_curator
  prompt_compressor --> prompt_regression_tester
  cache_planner --> llm_cost_estimator
  cache_planner --> context_engineer
  diff_summarizer --> review_triager
  diff_summarizer --> change_risk_scorer
  diff_summarizer --> change_narrator
  log_trimmer --> failure_analyst
  log_trimmer --> ci_doctor
  log_trimmer --> secrets_scanner
  model_router --> eval_designer
  model_router --> llm_cost_estimator
  retrieval_scoper --> context_budgeter
  retrieval_scoper --> module_mapper
  output_shaper --> tool_schema_designer
  output_shaper --> prompt_compressor
  session_summarizer --> memory_curator
  session_summarizer --> loop_planner
  tool_call_auditor --> memory_curator
  tool_call_auditor --> agent_auditor
  agent_auditor([agent-auditor · agent-ops])
  budget_controller([budget-controller · agent-ops])
  change_narrator([change-narrator · explainable-ai])
  change_risk_scorer([change-risk-scorer · risk-reward])
  ci_doctor([ci-doctor · devops])
  context_engineer([context-engineer · ai-engineering])
  eval_designer([eval-designer · agent-ops])
  failure_analyst([failure-analyst · agent-ops])
  llm_cost_estimator([llm-cost-estimator · ai-engineering])
  loop_planner([loop-planner · agent-ops])
  memory_curator([memory-curator · agent-ops])
  module_mapper([module-mapper · architecture])
  prompt_regression_tester([prompt-regression-tester · ai-engineering])
  review_triager([review-triager · code-review])
  secrets_scanner([secrets-scanner · safety])
  tool_schema_designer([tool-schema-designer · ai-engineering])
```
