<!-- Generated by tools/build.py from catalog/devops.toml. Edit the catalog, not this file. -->

# DevOps & Release

Keep CI green and fast, ship releases safely, and run production with good configs, containers, observability, and cost control.

```
/plugin marketplace add vishnu-77/clawmain
/plugin install devops@clawmain
```

Every agent follows the shared [loop contract](../../docs/loop-harness.md): plan, act, verify, reflect, with budgets, stop conditions and an evidence-backed report.

| Agent | Stage | Mode | Model | Why | Use when |
|---|---|---|---|---|---|
| `ci-doctor` | reflect | read-only | sonnet | green pipeline | CI is red, passes locally but fails in CI, or a workflow behaves unexpectedly |
| `build-accelerator` | plan | read-only | sonnet | faster feedback loop | builds or CI take too long |
| `release-notes-writer` | act | writes | haiku | users see changes | preparing a release, tagging a version, or publishing a changelog entry |
| `versioning-advisor` | plan | read-only | haiku | honest version numbers | choosing a version number, before tagging, or when version files disagree |
| `dockerfile-reviewer` | verify | read-only | sonnet | small safe images | writing or changing a Dockerfile or when images are large or slow |
| `env-config-auditor` | verify | read-only | haiku | config matches reality | adding config, when an environment behaves differently, or before a deploy |
| `deploy-checklist-runner` | verify | read-only | haiku | ready before deploy | about to deploy or release to production or staging |
| `observability-planner` | plan | read-only | sonnet | see problems early | shipping a new feature or service, when incidents were hard to debug, or when alerts are noisy |
| `incident-responder` | reflect | read-only | sonnet | calm fast triage | production is broken, degraded, or alerting, or right after an incident for a timeline |
| `infra-cost-reviewer` | reflect | read-only | sonnet | spend where valuable | cloud or CI bills grow, before scaling up, or when choosing instance sizes |

## Handoff graph

```mermaid
flowchart LR
  subgraph PLAN[plan]
    build_accelerator[build-accelerator]
    versioning_advisor[versioning-advisor]
    observability_planner[observability-planner]
  end
  subgraph ACT[act]
    release_notes_writer[release-notes-writer]
  end
  subgraph VERIFY[verify]
    dockerfile_reviewer[dockerfile-reviewer]
    env_config_auditor[env-config-auditor]
    deploy_checklist_runner[deploy-checklist-runner]
  end
  subgraph REFLECT[reflect]
    ci_doctor[ci-doctor]
    incident_responder[incident-responder]
    infra_cost_reviewer[infra-cost-reviewer]
  end
  ci_doctor --> flaky_test_hunter
  ci_doctor --> log_trimmer
  ci_doctor --> build_accelerator
  build_accelerator --> dockerfile_reviewer
  build_accelerator --> monorepo_organizer
  build_accelerator --> cache_planner
  release_notes_writer --> versioning_advisor
  release_notes_writer --> changelog_keeper
  versioning_advisor --> api_contract_reviewer
  versioning_advisor --> release_gatekeeper
  dockerfile_reviewer --> secrets_scanner
  dockerfile_reviewer --> supply_chain_auditor
  env_config_auditor --> secrets_scanner
  env_config_auditor --> deploy_checklist_runner
  deploy_checklist_runner --> release_gatekeeper
  deploy_checklist_runner --> rollback_planner
  deploy_checklist_runner --> migration_risk_reviewer
  observability_planner --> pii_detector
  observability_planner --> infra_cost_reviewer
  incident_responder --> rollback_planner
  incident_responder --> failure_analyst
  incident_responder --> premortem_facilitator
  infra_cost_reviewer --> cost_benefit_analyst
  infra_cost_reviewer --> blast_radius_mapper
  api_contract_reviewer([api-contract-reviewer · code-review])
  blast_radius_mapper([blast-radius-mapper · risk-reward])
  cache_planner([cache-planner · token-economy])
  changelog_keeper([changelog-keeper · docs])
  cost_benefit_analyst([cost-benefit-analyst · risk-reward])
  failure_analyst([failure-analyst · agent-ops])
  flaky_test_hunter([flaky-test-hunter · quality])
  log_trimmer([log-trimmer · token-economy])
  migration_risk_reviewer([migration-risk-reviewer · risk-reward])
  monorepo_organizer([monorepo-organizer · architecture])
  pii_detector([pii-detector · safety])
  premortem_facilitator([premortem-facilitator · risk-reward])
  release_gatekeeper([release-gatekeeper · risk-reward])
  rollback_planner([rollback-planner · risk-reward])
  secrets_scanner([secrets-scanner · safety])
  supply_chain_auditor([supply-chain-auditor · safety])
```
